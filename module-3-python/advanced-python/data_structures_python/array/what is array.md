# what is array ?

 1. array is not used as any data types in python 
 2. array is stored multiple data in a single variables 
 3. array is mutable 
 4. list used in python as array 


 **examples of array**
 ```
 emp=["brijesh","kumar","het"]
 print(emp)
 print(emp[1]) 

 ```

# numpy in python  :

  1. numpy are used in python as a library
  2. numpy used to create array 
  3. numpy is used to create array | 2d array | multidimentional array 
  4. numpy used to install pip install numpy 

  ```
  import numpy as np
  res=np.array([1,2,3,4,5,6])
  print(res)
  
  ```